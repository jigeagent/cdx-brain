# cdx-brain v0.9.0 全面测试报告

> 2026-06-27 | 康少
> 测试集：tests/test_phase4_full.py（290 行，105 项测试）

---

## 测试结果

```
总计: 105/105 (100.0%) ✅ 全部通过
```

## 各模块详

### Phase 4.1 反事实记忆 — 33/33 ✅

**核心覆盖**：
- 正则触发词 8 种组合：方案+放弃 / 路径+否决 / 方向+放弃 / 办法+不行 / 策略+不采用 / 架构+不可行 / 尝试+不行 / 试过+失效
- 否定守卫 4 例：放弃午休 / 先别放弃 / 还没放弃 / 不能放弃 — 全部正确拦截（按豹哥建议的保守策略）
- FTS5 存储：插入 → FTS5 搜索 → LIKE 后备搜索 → 列表 → 按主题筛选 → 统计
- inject_counterfactuals 输出格式：有匹配时含 ⚠️ 标记，无匹配时返回空
- promote_gate 联动：reject 时自动记录反事实

**关键设计验证**：否定语境守卫检测 20 字符前缀的"没/别/不会/不能/不想/不要/还没/暂时"，"暂时不用" 被正确拦截，避免"暂时不用管它"误报。

### Phase 4.2 哨兵监控 — 19/19 ✅

**核心覆盖**：
- 5 项单项检查：cache_size / OV health / BD sync / fragmentation / memory_stats
- quick check：3 项检查，<3 秒
- deep check：5 项检查，含 auto_fixed 机制
- format_report：Markdown 输出格式
- preflight_check：会话启动前预检

### Phase 4.3 任务森林 — 32/32 ✅

**核心覆盖**：
- TaskNode / TaskEdge 数据模型
- CRUD 操作：add_node / update_status / add_block / get_active
- Mermaid 可视化：输出 flowchart 格式
- prune TTL：30 天未更新的 blocked → cancelled
- stats 统计：total / by_status / active
- UserProfile：to_dict / from_dict 序列化
- handoff 交接：含/不含 session_summary 两种场景

**关键设计验证**：prune 只清理 30 天前未更新的 blocked 节点，不会误伤新节点。

### Hook 集成 — 21/21 ✅

**核心覆盖**：
- 5 个模板语法检查全部通过（无 SyntaxWarning）
- store.py 含 extract_counterfactual_from_text + Phase 4.1 标记
- inject.py 含 search_counterfactuals + cf_n 计数器
- compact.py 含 run_quick_check
- summary.py 含 run_quick_check
- 零 cc-star 残留（全库 grep 验证）
- 已安装 hooks 同步校验

---

## 测试用例速览

```
Phase 4.1 ── 33 项
├── 触发词匹配     8 项  (1.2.1–1.2.8)
├── 否定守卫       4 项  (1.3.1–1.3.4)
├── 复杂文本       5 项  (1.4.1–1.4.5)
├── 无匹配         3 项  (1.5.1–1.5.3)
├── FTS5 存储      8 项  (1.6.1–1.6.8)
├── 注入输出       3 项  (1.7.1–1.7.3)
└── gate 联动      1 项  (1.8)

Phase 4.2 ── 19 项
├── 单项检查       5 项  (2.1–2.5)
├── quick check    5 项  (2.6.1–2.6.5)
├── deep check     5 项  (2.7.1–2.7.5)
├── report         2 项  (2.8.1–2.8.2)
└── preflight      1 项  (2.9)

Phase 4.3 ── 32 项
├── 数据模型       4 项  (3.1–3.2)
├── CRUD 操作      7 项  (3.3.1–3.3.9)
├── Mermaid        2 项  (3.4.1–3.4.2)
├── Prune TTL      2 项  (3.5.1–3.5.2)
├── Stats          3 项  (3.6.1–3.6.3)
├── UserProfile    7 项  (3.7.1–3.7.7)
└── Handoff        5 项  (3.8.1–3.8.5)

Hook 集成 ── 21 项
├── 语法检查       5 项
├── API 引用       5 项
├── 零 cc-star     5 项
└── 已安装 hooks   5 项
```

---

## 测试文件

`E:\codex\cdx-brain\tests\test_phase4_full.py`
commit: 87c9d4a
运行方式：`python tests/test_phase4_full.py`
